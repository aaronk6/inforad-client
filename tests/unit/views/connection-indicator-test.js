import { moduleFor, test } from 'ember-qunit';

moduleFor('view:connection-indicator', 'Unit | View | connection indicator');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
