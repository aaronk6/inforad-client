import { moduleFor, test } from 'ember-qunit';

moduleFor('view:giphy-trending', 'Unit | View | giphy trending');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
