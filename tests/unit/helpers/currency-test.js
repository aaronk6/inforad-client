import {
  currency
} from '../../../helpers/currency';
import { module, test } from 'qunit';

module('CurrencyHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = currency(42);
  assert.ok(result);
});
